# Mission 1C — Closure Matrix

Every control from `docs/security/mission-1b-extreme-security-hardening-final-report.md`
classified `EXTERNAL_CONFIGURATION_REQUIRED` / `OWNER_DECISION_REQUIRED` /
`BLOCKED_DEPENDENCY` / `INTENTIONALLY_DEFERRED`, plus every control shipped
opt-in/disabled/report-only (i.e. code-complete but not actively enforced
anywhere). Built before any Mission 1C implementation, per section 2.

**Environment note governing every row below:** this session has no AWS
credentials for FirmsVault's real infrastructure account (`603013471426` —
only a Lightsail sandbox role in an unrelated account, `215302750105`, with
zero ECS/RDS permissions), and no staging origin resolves
(`app/client/admin.firmsvault.com` all NXDOMAIN) — no browser automation is
connected either. Every row whose "Verification method" says "requires real
AWS/browser access" is something this mission genuinely could not verify
live; see `docs/security/mission-1c-environment-constraints.md` for the full
detail. "Mission 1C action" states what THIS mission did about the gap given
that constraint — code-level work, a local proof where one was possible
(Docker/ClamAV), or an explicit unresolved flag.

| # | Control | Current state | Code complete? | Staging active? | Production active? | Owner decision? | Cost class | Verification method | Blocks MyAttorney? | Mission 1C action |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | Platform Admin WebAuthn browser flow | Crypto core fully verified (7/7 tests, real signatures); browser/Livewire integration never exercised against a real authenticator | Yes | No | No | No (verification, not a decision) | $0 | Real HTTPS origin + real browser + authenticator | **LAUNCH** (not build) | BLOCKED — no browser/staging origin available. Not fabricated. |
| 2 | Admin MFA policy: WebAuthn-only enforcement | Explicit policy: ≥1 factor required, WebAuthn preferred, TOTP independently sufficient | Yes | Yes (this IS the active policy) | Yes | Yes — revisit only after row 1 passes | $0 | Code review + `PlatformAdminMfaPolicyTest` | No | No change — correctly conditioned on row 1 |
| 3 | Firm-guard WebAuthn | Not built — hard-coupled to `PlatformAdmin` (table FK, relying-party host, type hints) | No | No | No | Yes — architecture decision (polymorphic credentials table + FORCE RLS + guard-aware RP host) | Dev time only | Code review | No (Firm WebAuthn is not a MyAttorney dependency) | Not attempted — correctly scoped as a distinct architecture project, not squeezed into this mission |
| 4 | Firm-user MFA mandatory enforcement | Blocked by a genuine lockout risk: `canAccessPanel()` denies the WHOLE panel with no in-panel enrollment path if `firm_user_2fa_mode=Required` | Partially — enrollment UI is Mission 1C's own scope (§5) | — | — | Yes — mandating is a firm-level product decision even after enrollment exists | $0 | Code review + new enrollment-flow tests | No | **Addressed this mission** — see task M1C-V3 |
| 5 | Firm role granularity (security/payment/trust admin roles) | Doesn't exist — only `FirmOwner/Attorney/Paralegal/LegalAssistant/Receptionist/BillingStaff` | No | No | No | Yes — product decision | Dev time only | Code review | No | Not attempted — inventing roles is a product decision, not a security-validation task |
| 6 | Step-up wired into every protected op | Wired into 2 real call sites (impersonation start, trust-account close); not into Firm Owner add/remove, Admin role changes, API key rotation, etc. | Mechanism yes; wiring partial | Yes (where wired) | Yes (where wired) | No | $0 | Code review | No | Not extended further this mission (out of the 27-section brief) |
| 7 | Session/device management UI | Revocation service exists; no self-service UI | Backend only | — | — | No — pure scope decision | Dev time only | Code review | No | Not built (not in the 27-section brief) |
| 8 | Session anomaly-detection alerting | No automated IP-change/impossible-travel signal | No | No | No | No — deliberately deferred per section 12's own caution | Dev time + alert infra | Code review | No | Not built |
| 9 | Export/large-upload rate limits | No route exists for either surface | N/A | N/A | N/A | No | $0 | N/A | No | Still N/A — no feature to rate-limit |
| 10 | AWS WAF (`module.waf`) | Terraform complete, managed rules + rate rules default COUNT mode, `enable_waf=false` | Yes | **No** | **No** | **Yes** — enabling + any COUNT→BLOCK flip | WAFv2: ~$5-6/mo base + ~$1/rule + $0.60/million requests (AWS list pricing order of magnitude, not a quote) | `terraform plan`/`apply` against the real account, then real traffic observation | **LAUNCH** | Not enabled — no AWS access to the real account (see environment note) |
| 11 | WAF Challenge/CAPTCHA | Not implemented | No | No | No | Deferred — needs real abuse data first | Additional WAF CAPTCHA cost | Requires row 10 live first | No | Not attempted |
| 12 | AWS Shield Advanced | Not purchased (Standard is the free default) | N/A | Standard: yes (AWS default) | Standard: yes | Yes — Advanced is a real paid decision | Shield Advanced: $3,000/mo minimum commitment (AWS list price) | AWS Console | No | Correctly not purchased, per section 17's explicit instruction |
| 13 | Content-Security-Policy | Real policy, ships `Content-Security-Policy-Report-Only` | Yes | Yes (report-only) | No | **Yes** — promoting to enforcing | $0 | Real browser session against every surface (§6), collect violations | **LAUNCH** | BLOCKED for real-browser proof — no staging origin/browser. Report-only left in place, not promoted. |
| 14 | Real malware-scanning engine | `VirusScanner` abstraction real; only implementation is `FakeVirusScanner` (disclosed stub) | Abstraction yes, engine no | No | No | Yes — provider/cost choice | Varies by provider (see M1C-V4 recommendation) | Local proof possible via Docker/ClamAV (no AWS needed) | **BUILD** (Client-facing document upload feature is unsafe without this) | **Addressed this mission** — see task M1C-V4, includes a real local proof |
| 15 | Document-download endpoint | Doesn't exist; `canAccess()` is firm-scoped only, no Client Portal grant check | Authorization primitive: partial | N/A | N/A | No — building the full endpoint is new feature work | Dev time only | Unit/feature tests against the primitive itself | **BUILD** (any future download feature needs this proven first) | **Addressed this mission** — proves the primitive only, not the full feature, see task M1C-V5 |
| 16 | Secrets rotation | Manual, via AWS Secrets Manager console/CLI | N/A (operational, not code) | — | — | Yes — rotation cadence/automation is an owner call | Secrets Manager rotation: ~$0.40/secret/mo + rotation Lambda invocations if automated | AWS Console/CLI | No | Not attempted — no AWS access, and rotating real production secrets blindly is explicitly prohibited by Mission 1B section 26 regardless |
| 17 | RDS security-group visibility | Partial — RDS predates this repo's Terraform, only referenced by ID | N/A | — | — | No — informational gap, not a decision | $0 | `aws rds describe-db-instances` + SG inspection against the real account | **LAUNCH** (informational) | BLOCKED — no AWS access to real account |
| 18 | Container image vulnerability scanning (ECR) | Unknown whether enabled at account level | N/A | Unknown | Unknown | Possibly (ECR enhanced scanning has cost implications) | ECR basic scanning: free; enhanced scanning (Inspector-based): per-image cost | AWS Console/CLI against real ECR repo | **LAUNCH** | BLOCKED — no AWS access |
| 19 | CloudTrail / GuardDuty / Security Hub / IAM Access Analyzer (`module.security_monitoring`) | Terraform complete, each behind its own `enable_*` flag, all default `false` | Yes | **No** | **No** | **Yes**, all four | CloudTrail: ~$2/100k mgmt events (first copy free) + S3 storage. GuardDuty: usage-based, order of $30-100+/mo depending on account activity (rough order of magnitude). Security Hub: ~$0.001/finding + $0.0010/check. IAM Access Analyzer: free. | `terraform apply` + AWS Console verification | **LAUNCH** | Not enabled — no AWS access to the real account |
| 20 | Security alerting delivery (where findings actually land) | No alert channel wired — `security_events` exists but nothing consumes it in real time | No | No | No | Depends on row 19 being enabled first | Depends on channel (SNS/email: near-$0; PagerDuty/Slack: existing tooling cost) | Trigger a real event, confirm it reaches a real destination | **LAUNCH** | Cannot prove without row 19 live + a real destination — documented as unresolved, see M1C final report |
| 21 | Bulk export protection | No export feature exists | N/A | N/A | N/A | No | $0 | N/A | No | Still N/A |
| 22 | `SuspendTrustAccountAction`/`OpenTrustAccountAction` step-up | Not step-up-gated (reversible actions, lower priority than Close) | No | — | — | No | $0 | Code review | No | Not extended this mission |
| 23 | AWS Backup (`module.backup`) | Terraform complete, KMS-encrypted vault + daily plan + tag-based selection, `enable_backup_plan=false` | Yes | **No** | **No** | **Yes** | AWS Backup: storage cost (~$0.05/GB-mo warm storage, varies by region) + restore costs if invoked | `terraform apply` + a real non-prod restore | **LAUNCH** | Not enabled — no AWS access. Restore proof additionally requires a real restore target, also blocked. |
| 24 | Restore testing | Never performed against real data | N/A | — | — | Depends on row 23 | Included in row 23's cost | Real non-production restore | **LAUNCH** | BLOCKED — depends on row 23 (not enabled) |
| 25 | Generic kill switches (pause-all-webhooks, pause-all-uploads, IP block) | Don't exist — only per-provider `ProviderKillSwitch` and the new session-revocation kill switch | No | — | — | No — scope decision, not cost | Dev time only | Code review | No | Not built (named as a known gap in the IR runbook, not silently hidden) |
| 26 | GitHub branch protection / required reviews / status checks | Unknown/unverifiable from this environment | N/A | — | — | Yes — real GitHub org/repo settings | $0 (GitHub free-tier feature) | GitHub repo settings UI/API with real org access | No (process control, not a runtime security gate) | Not attempted — no GitHub org-admin access from this session |
| 27 | Production OAuth secrets / DNS / TLS cutover | Not performed | N/A | — | — | Yes | Domain/cert costs likely near-$0 (ACM is free; Route53 has small monthly zone cost) | Real DNS/cert provisioning | No (pre-existing, unrelated to MyAttorney readiness) | Not attempted — explicitly out of scope per Mission 1B section 60 |
| 28 | Microsoft 365 disconnect provider-side revocation | Local token deletion only; Microsoft Graph-side revocation not implemented (or not confirmed) | Unconfirmed | — | — | No — implementation question, not cost | $0 | Real Microsoft Graph API test against a real tenant, or documented provider limitation | No (integration-specific, not a MyAttorney blocker) | **Addressed this mission** — see task M1C-V6 |
| 29 | DB TLS runtime proof (`DB_SSLMODE=require` actually in effect at runtime) | Confirmed as Terraform *configuration* (`shared_environment.DB_SSLMODE=require`); never confirmed via a live `pg_stat_ssl`/connection inspection against the real RDS instance | Config: yes. Runtime: unconfirmed | — | — | No | $0 | `SELECT * FROM pg_stat_ssl WHERE pid = pg_backend_pid();` against the real staging DB connection | **LAUNCH** | BLOCKED — no access to the real staging RDS instance from this sandbox |
| 30 | ECS public-IP-by-default | `assign_public_ip` defaults `true` because no NAT gateway exists in the real VPC; SG-level inbound restriction not independently re-verified live | Terraform: documented and gated. Live SG audit: not done | — | — | Yes — NAT gateway has a real recurring cost | NAT Gateway: ~$0.045/hr + ~$0.045/GB processed (AWS list price order of magnitude, ~$32+/mo per gateway before data) | Live VPC/SG/route-table inspection against the real account | **LAUNCH** | BLOCKED — no AWS access to inspect the real VPC |
| 31 | ECS read-only-root-filesystem | Terraform complete, `readonly_root_filesystem=false` default, never exercised against a running container | Yes | No (not enabled) | No | No — the flag itself is free; only unlocks after proof | $0 (no cost difference vs. current) | Local Docker exercise (no AWS needed) OR real ECS deploy | No (defense-in-depth, not a launch blocker) | **Addressed this mission** — see task M1C-V8, real local Docker proof |
| 32 | Security-audit event gaps (Firm-user MFA events, 2FA-mode changes, integration connect/disconnect, WebAuthn authenticator changes) | Partially covered — Platform Admin MFA fully audited, Firm-user MFA and integration-connect events are not routed to `security_events` | No | — | — | No | Dev time only | Code review + new tests | No | **Addressed this mission** — see task M1C-V7 |

## Summary counts

- **Code-complete but not active anywhere** (rows 10, 13, 19, 23, 31): 5 controls — WAF, CSP-enforcing, CloudTrail/GuardDuty/SecurityHub/AccessAnalyzer, Backup, read-only-root. Four of five require real AWS access this session doesn't have; the fifth (read-only-root) gets a genuine local proof this mission.
- **Owner decisions required, real cost implication**: rows 10, 12, 19, 23, 26 (GitHub is free-tier but still owner-side), 30 (NAT gateway).
- **Blocked purely by this session's lack of AWS/browser access** (not a code or design gap): rows 1, 10, 13 (verification half), 17, 18, 19, 20, 23, 24, 29, 30.
- **Genuinely addressed by Mission 1C's own achievable work**: rows 4, 14, 15, 28, 31, 32.
